import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
  postContainer: {
    display: "flex",
  
  },
  smMargin: {
    margin: theme.spacing(1),
  },
  actionDiv: {
    textAlign: "center",
  },
}));
